#!/usr/bin/env python3
"""Setup script for pcc - Python-to-native compiler.

This is provided for legacy build systems that don't yet support pyproject.toml.
Modern builds should use `pip install .` or `python -m build` instead.
"""

from setuptools import setup

setup()
